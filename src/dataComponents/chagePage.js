// import { pageNumber, setPageNumber, setSearchCharacter } from "./DataLogic";

// function changePage(type, number) {
//     setSearchCharacter("");
//     switch (type) {
//         case "next":
//             if (pageNumber < 9) setPageNumber(prevPageNumber => prevPageNumber + 1);
//             break;
//         case "previous":
//             if (pageNumber > 1) setPageNumber(prevPageNumber => prevPageNumber - 1);
//             break;
//         default:
//             setPageNumber(number);
//             break;
//     }
// }

// export { changePage };
